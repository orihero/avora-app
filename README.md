# avora

A new Flutter project.

## Appwrite / Login configuration

If you get **Failed host lookup: 'not-configured.invalid'** when logging in, set up Appwrite and your `.env` file (copy from `.env.example`). See [docs/APPWRITE_CONFIG.md](docs/APPWRITE_CONFIG.md).

`flutter run -d AKSC025715000898 --dart-define-from-file=.env`